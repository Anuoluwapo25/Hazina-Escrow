node_modules/
**/node_modules/
dist/
.env
*.log

# Local Stellar devnet artifacts — regenerated by `npm run devnet`.
# .env.devnet and devnet.accounts.json contain (throwaway, deterministic)
# secret keys for the ephemeral local network. See docs/DEVNET.md.
.env.devnet
devnet.accounts.json
data/devnet.datasets.json
data/devnet.db
test-results/
.DS_Store
frontend/dist/
backend/dist/
backend/backups/
backups/
test-backups/
backend/sqlite.db
backend/sqlite.db-shm
backend/sqlite.db-wal
backend/test.db
backend/test.db-shm
backend/test.db-wal
backend/data/hazina.db
backend/data/hazina.db-shm
backend/data/hazina.db-wal
backend/coverage/
frontend/coverage/
frontend/playwright-report/
frontend/test-results/

# Avoid committing property-test snapshot explosions (enable with `--features fuzz-tests` if needed).
contracts/hazina-escrow/test_snapshots/fuzz_tests/

# Added by cargo

/target
target_local/
.cargo-home/
fix.md
issue.md
pr.md

.claude

# Terraform - ignore local state, locks, and runtime files
# Local state files can contain sensitive values (API keys, secrets). Ensure these are not committed.
.terraform/
*.tfstate
*.tfstate.*
*.tfstate.backup
crash.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
terraform.tfvars
terraform.tfvars.json
.terraform.tfstate.lock.info
.terraform.lock.hcl
